/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'tqklhszfkvzk6518638.edge.naverncp.com',
            },
        ],
    },
};

export default nextConfig;
